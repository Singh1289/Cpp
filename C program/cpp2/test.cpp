#include <iostream>
#include <cmath>
using namespace std;

int main()
{
    int ma, rem, m = 6;

    cout << "\n divide :" << m / 28;
    cout << "\n divide round :" << round(m / 28);
    cout << "\n mode :" << m % 28;
    cout << "\n mode :" << round(m % 28);

    return 0;
}
